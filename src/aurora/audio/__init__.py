"""音訊播放與即時分析。

執行緒模型（很重要，改動前請先讀）::

    音訊回呼執行緒          UI 執行緒（Qt 主執行緒，60 Hz 輪詢）
    ─────────────          ──────────────────────────────
    施加音量增益            從環形緩衝取出新樣本
    混成單聲道              餵給 FFT／截止／響度／鼓點分析
    寫入環形緩衝            產生 SpectrumFrame
    更新純量狀態            讀取純量狀態並轉成 Qt signal

回呼執行緒只做 O(n) 的 numpy 運算，**絕不發 Qt signal、絕不做 FFT**。
所有訊號都由 UI 執行緒輪詢後才發出，避免跨執行緒 signal 風暴與音訊斷續。
"""
